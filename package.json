{
  "name": "tamper-proof-certificate-verification",
  "version": "1.0.0",
  "description": "Blockchain-based certificate verification system",
  "main": "api/index.js",
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
    "build": "cd client && npm install && npm run build",
    "vercel-build": "npm install --prefix server && cd client && npm install && npm run build",
    "test": "echo \"No tests specified\"",
    "start": "cd server && npm start"
  },
  "keywords": [
    "blockchain",
    "certificate",
    "verification",
    "ethereum",
    "ipfs",
    "react",
    "nodejs"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.7.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.19.0"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^3.1.10"
  }
}