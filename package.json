{
  "name": "tamper-proof-certificate-verification",
  "version": "1.0.0",
  "description": "Blockchain-based certificate verification system",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../server && npm install && cd ../blockchain && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
    "build": "cd client && CI=false npm run build",
    "vercel-build": "cd client && npm install && CI=false npm run build",
    "test": "cd blockchain && truffle test",
    "start": "cd client && npm start"
  },
  "keywords": [
    "blockchain",
    "certificate",
    "verification",
    "ethereum",
    "ipfs",
    "react",
    "nodejs"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}