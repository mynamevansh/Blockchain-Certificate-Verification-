# MongoDB Atlas Configuration Template
# 1. Replace <username> with your MongoDB Atlas database username
# 2. Replace <password> with your MongoDB Atlas database password  
# 3. Replace <cluster-url> with your Atlas cluster URL (e.g., cluster0.abc123.mongodb.net)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/certificate_verification?retryWrites=true&w=majority

# Example (DO NOT use these credentials):
# MONGODB_URI=mongodb+srv://myuser:mypassword@cluster0.abc123.mongodb.net/certificate_verification?retryWrites=true&w=majority

DB_NAME=certificate_verification

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_in_production_2024_blockchain_cert
JWT_EXPIRES_IN=24h

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration (Update to match your frontend URL)
CLIENT_URL=http://localhost:3001