const mongoose = require('mongoose');const bcrypt = require('bcryptjs');require('dotenv').config();const User = require('./models/User');async function resetStudentPasswords() {  try {    await mongoose.connect(process.env.MONGO_URI);    console.log('✅ Connected to MongoDB');    const students = [      { studentId: 'STU-2024-001', email: 'vansh@university.edu', password: 'vansh123' },      { studentId: 'STU-2024-002', email: 'shashank@university.edu', password: 'shashank123' },      { studentId: 'STU-2024-003', email: 'shreyas@university.edu', password: 'shreyas123' }    ];    for (const studentData of students) {      const user = await User.findOne({ studentId: studentData.studentId });      if (user) {        user.email = studentData.email;        user.password = studentData.password;         await user.save();        console.log(`✅ Updated ${user.name}: ${studentData.email} / ${studentData.password}`);      } else {        console.log(`❌ Student ${studentData.studentId} not found`);      }    }    console.log('\n✅ All student passwords have been reset!');    console.log('\n📋 Login Credentials:');    console.log('  Vansh:    vansh@university.edu    / vansh123');    console.log('  Shashank: shashank@university.edu / shashank123');    console.log('  Shreyas:  shreyas@university.edu  / shreyas123');    process.exit(0);  } catch (error) {    console.error('Error:', error);    process.exit(1);  }}resetStudentPasswords();