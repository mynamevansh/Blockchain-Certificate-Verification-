const { ethers } = require("hardhat");require("dotenv").config();async function verifyCertificate(certificateId) {  try {    console.log("🔍 Certificate Verification Process\n");    console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");    const contractAddress = process.env.CONTRACT_ADDRESS;    if (!contractAddress) {      throw new Error("CONTRACT_ADDRESS not set in .env");    }    const contractABI = require("../CertificateVerification.abi.json");    const provider = ethers.provider;    const contract = new ethers.Contract(contractAddress, contractABI, provider);    console.log("Contract:", contractAddress);    console.log("Certificate ID:", certificateId);    console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n");    const cert = await contract.verifyCertificate(certificateId);    const statusMap = {      0: "Unknown (Not Found)",      1: "Active ✓",      2: "Revoked ✗"    };    console.log("📋 Certificate Information:");    console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");    console.log("Certificate Hash:", cert.certificateHash);    console.log("IPFS CID:", cert.ipfsCID);    console.log("Student ID:", cert.studentId);    console.log("Student Name:", cert.studentName);    console.log("Issuer Address:", cert.issuerAddress);    console.log("Issued At:", new Date(Number(cert.issuedAt) * 1000).toISOString());    console.log("Status:", statusMap[cert.status]);    console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n");    const isActive = await contract.isActive(certificateId);    console.log("Is Active:", isActive ? "✅ Yes" : "❌ No");    if (cert.ipfsCID) {      console.log("\n🔗 IPFS Links:");      console.log(`https:      console.log(`https:    }    return cert;  } catch (error) {    console.error("\n❌ Verification failed:");    console.error(error.message);    throw error;  }}async function main() {  const certificateId = process.argv[2] || "CERT-2024-CS-001";  await verifyCertificate(certificateId);}if (require.main === module) {  main()    .then(() => process.exit(0))    .catch((error) => {      console.error(error);      process.exit(1);    });}module.exports = { verifyCertificate };