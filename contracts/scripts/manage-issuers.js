const { ethers } = require("hardhat");require("dotenv").config();async function addIssuer(issuerAddress) {  try {    console.log("➕ Adding Authorized Issuer\n");    console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");    const [owner] = await ethers.getSigners();    console.log("Owner Address:", owner.address);    const contractAddress = process.env.CONTRACT_ADDRESS;    const contractABI = require("../CertificateVerification.abi.json");    const contract = new ethers.Contract(contractAddress, contractABI, owner);    console.log("New Issuer:", issuerAddress);    console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n");    const isAuthorized = await contract.isAuthorizedIssuer(issuerAddress);    if (isAuthorized) {      console.log("⚠️  Address is already an authorized issuer");      return;    }    console.log("📤 Sending transaction...");    const tx = await contract.addIssuer(issuerAddress);    console.log("Transaction Hash:", tx.hash);    await tx.wait();    console.log("✅ Issuer added successfully!\n");    return tx;  } catch (error) {    console.error("❌ Error:", error.message);    throw error;  }}async function removeIssuer(issuerAddress) {  try {    console.log("➖ Removing Authorized Issuer\n");    console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");    const [owner] = await ethers.getSigners();    const contractAddress = process.env.CONTRACT_ADDRESS;    const contractABI = require("../CertificateVerification.abi.json");    const contract = new ethers.Contract(contractAddress, contractABI, owner);    console.log("Removing Issuer:", issuerAddress);    console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n");    const tx = await contract.removeIssuer(issuerAddress);    await tx.wait();    console.log("✅ Issuer removed successfully!\n");    return tx;  } catch (error) {    console.error("❌ Error:", error.message);    throw error;  }}async function getStats() {  try {    const contractAddress = process.env.CONTRACT_ADDRESS;    const contractABI = require("../CertificateVerification.abi.json");    const contract = new ethers.Contract(contractAddress, contractABI, ethers.provider);    const stats = await contract.getStatistics();    const owner = await contract.owner();    console.log("📊 Contract Statistics\n");    console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");    console.log("Contract Address:", contractAddress);    console.log("Contract Owner:", owner);    console.log("Total Issued:", stats.totalIssued.toString());    console.log("Total Revoked:", stats.totalRevoked.toString());    console.log("Total Active:", stats.totalActive.toString());    console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n");  } catch (error) {    console.error("❌ Error:", error.message);    throw error;  }}async function main() {  const command = process.argv[2];  const address = process.argv[3];  if (command === "add" && address) {    await addIssuer(address);  } else if (command === "remove" && address) {    await removeIssuer(address);  } else if (command === "stats") {    await getStats();  } else {    console.log("Usage:");    console.log("  node manage-issuers.js add <ADDRESS>");    console.log("  node manage-issuers.js remove <ADDRESS>");    console.log("  node manage-issuers.js stats");  }}if (require.main === module) {  main()    .then(() => process.exit(0))    .catch((error) => {      console.error(error);      process.exit(1);    });}module.exports = { addIssuer, removeIssuer, getStats };