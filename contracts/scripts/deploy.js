const hre = require("hardhat");async function main() {  console.log("🚀 Starting deployment of CertificateVerification contract...\n");  const [deployer] = await hre.ethers.getSigners();  console.log("📝 Deployment Details:");  console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");  console.log("Deploying with account:", deployer.address);  const balance = await hre.ethers.provider.getBalance(deployer.address);  console.log("Account balance:", hre.ethers.formatEther(balance), "ETH");  const network = await hre.ethers.provider.getNetwork();  console.log("Network:", network.name);  console.log("Chain ID:", network.chainId.toString());  console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n");  console.log("📦 Compiling contract...");  const CertificateVerification = await hre.ethers.getContractFactory("CertificateVerification");  console.log("🔨 Deploying contract to the blockchain...");  const contract = await CertificateVerification.deploy();  await contract.waitForDeployment();  const contractAddress = await contract.getAddress();  console.log("\n✅ Deployment Successful!\n");  console.log("📋 Contract Information:");  console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");  console.log("Contract Address:", contractAddress);  console.log("Contract Owner:", deployer.address);  console.log("Block Explorer:", getBlockExplorerUrl(network.chainId, contractAddress));  console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n");  console.log("🔍 Verifying initial contract state...");  const owner = await contract.owner();  const isAuthorized = await contract.authorizedIssuers(deployer.address);  const totalCerts = await contract.totalCertificatesIssued();  console.log("Owner address:", owner);  console.log("Deployer is authorized issuer:", isAuthorized);  console.log("Total certificates issued:", totalCerts.toString());  console.log("✓ Initial state verified\n");  const fs = require("fs");  const deploymentInfo = {    network: network.name,    chainId: network.chainId.toString(),    contractAddress: contractAddress,    deployerAddress: deployer.address,    deploymentTime: new Date().toISOString(),    blockExplorer: getBlockExplorerUrl(network.chainId, contractAddress),    contractName: "CertificateVerification",    compiler: "v0.8.19+",    verified: false  };  const deploymentPath = `./deployments/deployment-${network.name}-${Date.now()}.json`;  fs.mkdirSync("./deployments", { recursive: true });  fs.writeFileSync(deploymentPath, JSON.stringify(deploymentInfo, null, 2));  console.log("💾 Deployment info saved to:", deploymentPath);  const envPath = ".env.deployment";  const envContent = `# CertificateVerification Contract Deployment# Network: ${network.name}# Deployed: ${new Date().toISOString()}CONTRACT_ADDRESS=${contractAddress}NETWORK=${network.name}CHAIN_ID=${network.chainId}DEPLOYER_ADDRESS=${deployer.address}BLOCK_EXPLORER=${getBlockExplorerUrl(network.chainId, contractAddress)}`;  fs.writeFileSync(envPath, envContent.trim());  console.log("💾 Contract address saved to:", envPath, "\n");  console.log("📝 Next Steps:");  console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");  console.log("1. Update your frontend with contract address:", contractAddress);  console.log("2. Verify contract on block explorer (see instructions below)");  console.log("3. Test contract functions using the provided scripts");  console.log("4. Add additional issuers if needed using addIssuer()");  console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n");  if (network.chainId !== 31337n) {     console.log("🔐 To verify contract on Etherscan:");    console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");    console.log(`npx hardhat verify --network ${network.name} ${contractAddress}`);    console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n");  }  console.log("✨ Deployment complete!\n");  return {    contract,    contractAddress,    deployer: deployer.address,    network: network.name  };}function getBlockExplorerUrl(chainId, address) {  const explorers = {    1: `https:    5: `https:    11155111: `https:    137: `https:    80001: `https:    56: `https:    97: `https:  };  return explorers[chainId.toString()] || `Chain ID ${chainId}: ${address}`;}main()  .then(() => process.exit(0))  .catch((error) => {    console.error("\n❌ Deployment failed:");    console.error(error);    process.exit(1);  });module.exports = { main };