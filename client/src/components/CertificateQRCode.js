import React, { useState } from 'react';import { QRCodeSVG } from 'qrcode.react';import { QrCode, X } from 'lucide-react';const CertificateQRCode = ({ certificateId, baseUrl = window.location.origin }) => {  const [showQR, setShowQR] = useState(false);  const verifyUrl = `${baseUrl}/verify/${certificateId}`;  return (    <>      <button        onClick={() => setShowQR(true)}        style={{          padding: '0.5rem',          backgroundColor: 'transparent',          color: '#64748b',          border: '1px solid #cbd5e1',          borderRadius: '8px',          cursor: 'pointer',          display: 'flex',          alignItems: 'center',          justifyContent: 'center',          transition: 'all 0.2s'        }}        title="Show QR Code"      >        <QrCode size={16} />      </button>      {showQR && (        <div style={{          position: 'fixed',          top: 0,          left: 0,          right: 0,          bottom: 0,          backgroundColor: 'rgba(0, 0, 0, 0.5)',          display: 'flex',          alignItems: 'center',          justifyContent: 'center',          zIndex: 2000        }}>          <div style={{            backgroundColor: 'white',            borderRadius: '12px',            padding: '2rem',            maxWidth: '400px',            width: '90%',            boxShadow: '0 10px 25px rgba(0, 0, 0, 0.2)',            textAlign: 'center'          }}>            <div style={{              display: 'flex',              justifyContent: 'space-between',              alignItems: 'center',              marginBottom: '1rem'            }}>              <h3 style={{                fontSize: '1.25rem',                fontWeight: '600',                color: '#1e293b',                margin: 0              }}>                Scan to Verify Certificate              </h3>              <button                onClick={() => setShowQR(false)}                style={{                  padding: '0.5rem',                  backgroundColor: 'transparent',                  border: 'none',                  cursor: 'pointer',                  color: '#64748b'                }}              >                <X size={20} />              </button>            </div>            <div style={{              display: 'flex',              justifyContent: 'center',              padding: '1rem',              backgroundColor: '#f8fafc',              borderRadius: '8px',              marginBottom: '1rem'            }}>              <QRCodeSVG                value={verifyUrl}                size={256}                level="H"                includeMargin={true}              />            </div>            <p style={{              fontSize: '0.875rem',              color: '#64748b',              marginBottom: '0.5rem'            }}>              Certificate ID: <strong>{certificateId}</strong>            </p>            <p style={{              fontSize: '0.75rem',              color: '#9ca3af',              margin: 0            }}>              Scan this QR code to verify the certificate authenticity            </p>          </div>        </div>      )}    </>  );};export default CertificateQRCode;