import React from 'react';import { CheckCircle, XCircle, AlertCircle } from 'lucide-react';const StatusBadge = ({ status, size = 'md' }) => {  const statusConfig = {    'Valid': {      icon: <CheckCircle size={size === 'sm' ? 12 : 16} />,      bgColor: 'bg-green-50',      textColor: 'text-green-700',      borderColor: 'border-green-200',      label: 'Active'    },    'Active': {      icon: <CheckCircle size={size === 'sm' ? 12 : 16} />,      bgColor: 'bg-green-50',      textColor: 'text-green-700',      borderColor: 'border-green-200',      label: 'Active'    },    'Revoked': {      icon: <XCircle size={size === 'sm' ? 12 : 16} />,      bgColor: 'bg-red-50',      textColor: 'text-red-700',      borderColor: 'border-red-200',      label: 'Revoked'    },    'revoked': {      icon: <XCircle size={size === 'sm' ? 12 : 16} />,      bgColor: 'bg-red-50',      textColor: 'text-red-700',      borderColor: 'border-red-200',      label: 'Revoked'    },    'Pending': {      icon: <AlertCircle size={size === 'sm' ? 12 : 16} />,      bgColor: 'bg-yellow-50',      textColor: 'text-yellow-700',      borderColor: 'border-yellow-200',      label: 'Pending'    }  };  const config = statusConfig[status] || statusConfig['Pending'];  const sizeClasses = size === 'sm' ? 'px-2 py-1 text-xs' : 'px-3 py-1.5 text-sm';  return (    <span      className={`inline-flex items-center gap-1.5 ${sizeClasses} font-semibold rounded-full border ${config.bgColor} ${config.textColor} ${config.borderColor} transition-all duration-200`}    >      {config.icon}      {config.label}    </span>  );};export default StatusBadge;